# Kuron Desktop — Project Memory

> **Unified context file** for tracking progress across AI tools.
> Read by: **Codex** | **OpenCode** | **Manual Review**

---

## Project Identity

| Key | Value |
|---|---|
| **App Name** | **Kuron Desktop** |
| **Repo** | `kuron-desktop` (lokal, belum push) |
| **Platform** | Pure desktop win+mac+linux GUI (Tauri v2) |
| **Stack** | Rust + TypeScript + Vite 8 + Svelte 5.57 |
| **Version** | 0.1.0 (`id.nhasix.kuron`) |
| **Architecture** | Clean Architecture (Domain → Data → Presentation via IPC) |
| **State Management** | Svelte 5 runes stores (`*.svelte.ts`), ganti Cubit |
| **DI** | `tauri::Builder::manage(AppState)` (`Arc<dyn ContentRepository>`), ganti GetIt |
| **Networking** | `HttpClientManager` reqwest rustls + cookie jar + DoH JSON + UA/rate-limit/retry per config |
| **Routing** | `lib/router/route.svelte.ts` (`splash` \| `main`), SvelteKit Fase 4 |
| **Database** | `rusqlite` bundled (contents/chapters/downloads/translation_cache/history/favorites) + KV JSON + keychain |
| **Logging** | `tracing` subscriber (`RUST_LOG`, default info; tanpa log prompt/gambar) |
| **Sources** | Bundel HANYA nhentai; sisanya install via manifest-link/zip `kuron-extensions` (`ExtensionManager` + sha256) |

### Related Repos
| Repo | Role |
|---|---|
| `shirokun20/kuron-mobile` | Ibu: assets, DESIGN.md, pola UI (splash/main/cards) |
| `KURON_TAURI_V2_DESKTOP.md` | Spec arsitektur → `docs/architecture.md` |

### Key Features
- Splash → Main flow (init appInfo + feed saat splash, window resize 480×700 → 1200×800)
- App-shell NeedMCP (sidebar 256 + header 64 + konten 1180; tab sumber dihapus, sumber via sidebar)
- Material rasa Kuron: Light/Dark solid flat-hairline (dial Linen Meridian), tag palette 12 kategori
- Backend real Fase 2: network, SQLite/KV/keychain, cache, adapter scraper/REST + NHentai API live
- Ekstensi installable 1:1 mobile (bundel nhentai-only, manifest + sha256 + zip)
- Offline-first + AI translate reader (roadmap Fase 5/6, reuse Rust mobile)

---

## Architecture Overview

```
src-tauri/src/
├── core/              # AppError (Network/Storage), AppState DI Arc<dyn>, config, constants, logger(tracing)
├── domain/            # Pure Rust — serde + ts-rs (ADR-004); entities, repo traits async, value objects, services
│   ├── entities/      # hello, content, chapter, page_image_result, download_task,
│   │                  # ai_translation, glossary, search_filter, reader_settings (+roundtrip tests)
│   ├── repositories/  # ContentRepository/HomeFeedRepository async + blanket Arc
│   ├── value_objects/ # SourceId, Language
│   ├── services/      # title_parser
│   └── ts_export.rs   # test generate `src/lib/domain/types.ts` (15 tipe)
├── application/       # UseCases 1 file 1 execute async (say_hello, get_home_feed, search,
│   │                  # get_content_detail, get_chapters, get_page_images) + services/rate_limiter
├── data/              # MockContentRepository + ContentRepositoryImpl (remote-first)
│   ├── models/        # ContentModel (From Entity)
│   ├── datasources/   # config (SourceFile/overlay) + extension (manifest/sha256/zip)
│   │                  # + local (rusqlite/KV/keychain/file) + remote (scraper/REST/NHentai API)
│   ├── repositories/  # mock + real impl
│   └── native/        # image_ops stub (port kuron_native/rust Fase 3)
├── commands/          # Thin IPC: hello_world, app_info, home_feed, search, get_detail,
│   │                  # get_chapters, get_page_images
├── network.rs         # HttpClientManager (rustls/jar/retry) + DoH JSON + kuron_user_agent
├── cache.rs           # ImageCache (`$sourceId/$contentId/page_N.jpg` via FileCacheDs)
└── resources/         # source-configs/ inti (nhentai + mangadex/ehentai/hitomi)

src/lib/
├── api/               # client.ts (typed invoke) + window.ts (resize + openPopup generik)
├── domain/            # types.ts GENERATED ts-rs (jangan edit manual)
├── stores/            # hello, content, source, theme (runes, ganti Cubit/Bloc)
├── router/            # route.svelte.ts, SvelteKit Fase 4
├── theme/             # tokens.ts (port colors/design_tokens/tag palette 1:1)
├── components/        # Sidebar, SourceList, TagChip, MainGridCard, MainFeaturedCard, ThemeToggle
└── pages/             # SplashScreen, MainPage, SourcePickerPage, AboutPage
```

### Layer Rules
- **Domain**: Pure Rust + `serde` + `ts-rs` (ADR-004). No Tauri/SQLite/reqwest.
- **Data**: Depends only on Domain. Remote/local real (mock hanya default AppState).
- **Application**: Depends only on Domain. Satu use case = satu file + `execute` async.
- **Presentation**: Svelte via `invoke('cmd_*')` only. Commands thin, `AppError -> String` di boundary.
- **Sources**: JSON config mobile (bundle inti nhentai+mangadex/ehentai/hitomi agar search/filter config-driven; sisanya installable + sha256, overlay menang).

---

## Current Progress Dashboard

> Tracked via `openspec/` — Last updated: 2026-09-20 (20/37 tasks, change `kuron-desktop-roadmap` in-progress)

### Archived (in `openspec/changes/archive/`)
- *(none)*

### Active Changes (in `openspec/changes/`)
- `kuron-desktop-roadmap` — proposal + 13 spec + design + tasks (20/37 DONE: grup 1–3, 4.1–4.6)
- `kuron-parity-1to1` — change turunan parity 1:1 mobile (proposal + 5 spec + design + tasks A–E, PLANNED; `openspec validate` valid 0 warning)

### Open Issues (tech debt tercatat, sadar)
- `MockContentRepository` masih default AppState — wiring real (installed) pending 4.7/Fase 6
- `SOURCES`/`SOURCE_META` hardcode — daftar dinamis pending 4.7
- HTML NHentai/Hitomi/EH live 403 CF-edge — cookie-harvest pending (API v2 jadi jalur NHentai)
- CI `ci.yml` menunggu push pertama untuk hijau di 3 runner
- `KosugiMaru.ttf` 3.5MB di bundle — subset/lazy-load Fase 8
- SPA + route store manual — SvelteKit Fase 4
- Signing/notarize + updater — Fase 8

---

### Recent Sessions

> Session log in this table. Last updated: 2026-09-20.

| Date | Agent | Area | Status | Summary |
|---|---|---|---|---|
| 2026-09-21 | Muse | Detail terkait/komentar + fix 404 nhentai | Done | User lapor reader nhentai 404 (`/galleries/https://...`): `gallery_of_chapter` tak tangani `external_url` penuh → ekstrak digit `/g/{id}/` + regresi input user (62/0/0). User benar: detail punya endpoint khusus — implementasi: entity `Comment`+`Tag`, `Content.tags`, trait `get_related_content/get_comments`, adapter nhentai (`related` config + `comments` embedded, bentuk live diverifikasi curl galeri 682821), 2 usecase + 2 command, DetailPage seksi Terkait (rail klik) + Komentar (teks, non-blocking paralel ala cubit mobile) + chips tag; sumber lain kosong jujur. E2E menemukan bug store (riwayat tak refresh pasca-rekam) → upsert optimistik. E2E stubbed Chrome headless (mockIPC resmi + CDP, harness `/tmp/kuron-e2e`, butuh eskalasi bind): **17/17 PASS** + screenshot identitas. `cargo` 65/0/0, `pnpm` 0/0 + 21 pass + build OK. needmcp: TIDAK tersedia di sesi (tanpa CLI/key/config) — identitas dikerjakan langsung (taste-skill + token Kuron). |
| 2026-09-21 | Muse | Rantai baca + reader v0 (7.0) | Done | User: riwayat tak ada jalan masuk tanpa detail/chapters/reader. Fakta: backend detail/chapters/pages real sudah ada, tapi 3 command mock-only (bug sekelas 4c) + kartu tak bisa diklik. Fix: 3 command + param `source` → `repo_for`; kartu klik → DetailPage (info+bab+Mulai Baca+favorit, eksternal→browser) → ReaderPage v0 (scroll+spy halaman+rekam throttle 2 dtk ala mobile, flush saat keluar); riwayat/favorit bisa dibuka ulang. Svelte: `article`→`div` role button (aturan a11y), derived-fetch (anti state warning). `cargo` 61/0/0, `pnpm` 0/0 + build OK. tasks 7.0 DONE (7.1–7.3 penuh tetap PLANNED). Perlu restart `pnpm dev` (command Rust berubah). |
| 2026-09-21 | Muse | Halaman Library + riwayat snapshot | Done | User: favorit/riwayat tak terlihat tanpa UI + bukti catatan tasks. Fakta: item sidebar Favorit/Riwayat sudah ada tapi dead-end (`activeNav` tak pernah ganti konten). Fix: `LibraryPage` (grid favorit reuse kartu + baris riwayat cover/judul/halaman/tanggal + hapus per-item + hapus riwayat + reset confirm) + cabang `activeNav` di MainPage; backend: `record_history` simpan snapshot `Content` ala mobile, `HistoryEntry`→`HistoryItem` display-ready, `cmd_history_remove` baru; `bigint` ts-rs di fmtDate. `cargo` 61/0/0, `pnpm check` 0/0, build OK. Pemicu catat-riwayat (reader/detail) tetap Fase 5. |
| 2026-09-21 | Muse | Nol-fail/nol-ignore + slice 8.3-easy | Done | Test hygiene penuh: 6 test bind-socket → murni offline (ekstrak `parse_detail/parse_chapters/parse_page_images`, kebijakan retry murni, `ExtensionManifest::parse` + `install_body`), 9 `#[ignore]` → 3 test offline baru (mapping mangaTags, parse DoH, secret memori) + 9 live-test di feature `live-tests` (opt-in, hermetik temp-dir, config-driven tanpa bundle). Bonus: drift `ZipPreview` (export list ts-rs lupa) → `pnpm check` 0/0; `record_history` delete+insert ala sqflite-replace (recency benar sedetik sama). Hasil: `cargo test --lib` **61 pass/0 fail/0 ignored**, 0 warning, `pnpm check` 0/0, `pnpm test` 15 pass, `vite build` OK, `openspec validate --all` 16/16. Slice 8.3-easy: `LibraryRepository` + impl + 6 usecase + 6 command + DI `AppState.library` (`kuron.db`, fallback memori) + store + toggle hati kartu. SISA jujur: entity `History` penuh 14 field + halaman Library + koleksi → Fase 5/6; suite live belum dijalankan (butuh internet di luar sandbox). |
| 2026-09-21 | Muse | Parity 1:1 Propose | Done | Explore roadmap vs `kuron_architecture_for_rust.md` → gap besar (generic-depth, kuron_special 9 source, reader pipeline, download/offline). Keputusan user: change turunan + semua gap + adaptasi desktop wajar. Tulis `kuron-parity-1to1` (proposal+design+tasks A–E+5 spec: content-browse, kuron-special, reader, offline-download, parity-scope); 32 warning SHALL diperbaiki; `openspec validate` valid 0 warning. Install global `@fission-ai/openspec@1.13.1` (lokal project kosong). |
| 2026-09-20 | Muse | OpenSpec sync | Done | Synced all 14 active `kuron-desktop-roadmap` delta specs into `openspec/specs/`; `openspec validate --specs` passed 14/14. |
| 2026-09-20 | Muse | Extension URL inputs | Done | Manifest URL and ZIP URL inputs now start empty; manifest loading is user-triggered and validates a required URL before fetch/install. |
| 2026-09-20 | Muse | Filter tag picker | Done | Quiet browser/native repro confirmed `mangaTags:77`; picker DOM renders 77 options and include/exclude cycling works. |
| 2026-09-20 | Muse | Ikon sumber di Sidebar (`ui.iconPath`/`ui.displayName`) | Done | User request: Sidebar cuma tile inisial. Akar: `source_entries()` ambil `icon_url` dari `{id}-meta.json` saja → sumber bundled/local tanpa ikon (meta hanya ada hasil install). Fix: (1) `SourceFile.ui` disimpan sebagai `serde_json::Value` **lenient** + helper `ui_icon_path()`/`ui_display_name()` — pola `navigation`/`search_form`; struct strict (`deny_unknown_fields`) sempat MEMATIKAN parse config ehentai ter-install di data dir user karena blok `ui`-nya ada `brandColor` (bug laten, ketemu pas test; nhentai aman karena field-nya kebetulan semua dikenal). (2) `source_entries()` + `cmd_extension_install`: ikon = meta.json (install) → `ui.iconPath` config → None; `display_name` ikut dikirim. (3) FE: `SourceMeta` + `sourceStore.currentIconUrl`, label dari `ui.displayName` (fallback `prettyLabel`), Sidebar render `<img>` + fallback inisial (reset via `$effect` saat sumber ganti); `types.ts` regen otomatis via `cargo test --lib` (ts-rs). Ikon nhentai bundel kini URL remote GitHub (commit `1a63e61`). Tes +5 (di.rs ×3: bundled/installed-override/tanpa-ui; config.rs ×2: `brandColor` tetap parse + `ui` absen). Verifikasi: `cargo test --lib` **52 pass/9 ignored**, `pnpm check` 0/0, `pnpm test` 15 pass, `pnpm build:frontend` OK (185 kB). Sebagian besar fix Rust + offline UX sudah masuk commit `ac2fae7`; sisa (tes config.rs + Sidebar/MainPage/source.svelte.ts) belum di-commit. |
| 2026-09-20 | Muse | Bug: "gagal muat feed: mode web…" + lanjut proses terhenti | Done | Proses terhenti (validasi AboutPage, dsb.) sudah terangkat utuh di commit `08f1f5a`; tinggal validasi. Bug: error muncul karena feed dibuka lewat `http://localhost:1420` di **browser** — `cmd_home_feed` hanya ada di proses Tauri (tidak ada server REST di backend), jadi `call()` menolak dengan `WEB_OFFLINE_MSG`. Fix UX, bukan wiring: (1) teks web-offline tak lagi menuduh `pnpm dev` (menyesatkan — backend Tauri tak terjangkau tab browser mana pun); (2) `contentStore.offline` (flag, bukan `error` merah; `fail()` cocokkan `WEB_OFFLINE_MSG`, `loadMore` guard); (3) MainPage: panel "Mode Web — tanpa backend" rapi (penjelasan + tombol Pilih Sumber/Coba Muat Ulang), banner "gagal muat feed" khusus error backend sungguhan, searchbar disembunyikan saat offline. Verifikasi: `pnpm check` 0/0, `pnpm test` 15 pass, `cargo test --lib` 47 pass/9 ignored, `pnpm build:frontend` OK, kompilasi dev-mode Vite 200 + marker offline terdeteksi. Catatan: buka UI di browser = memang tanpa data nyata; kalau butuh data di web → server REST terpisah (belum ada). |
| 2026-09-20 | Muse | Kondisi platform: desktop / web / mobile satu pintu tap | Done | User: buat kondisi supaya browser/mobile/desktop **tidak** beda cara tap + bersihkan yang masih menempel ke window JS. Akar: Sidebar `openSourcePicker()`, MainPage `openAboutWindow/openExtensionManager/openFilterWindow`, dan emit/close di halaman popup semua jalur Tauri-only → di web error "Tauri window API tidak tersedia" / "emit gagal". Fix: (1) `api/platform.ts` MURNI (`isTauriRuntime`, `isNarrowViewport` ≤900px, `platformMode` desktop\\|web\\|mobile, `canUseNativeWindows`, `subscribePlatform`) + `stores/platform.svelte.ts` reaktif + 8 tes node; (2) `stores/overlay.svelte` (satu pintu: desktop Tauri layar lebar → popup native `openWindowPopup(kind)`, web/mobile → overlay in-app; tap jenis sama = toggle) + `components/OverlayHost.svelte` (sheet di lebar, full-screen di mobile; header+✕ sendiri kecuali Filter); (3) pemanggil dibersihkan: Sidebar & MainPage pakai `overlayStore.open`, `api/window.ts` guard `canUseNativeWindows()`, `SourcePickerPage`/`FilterPage` dua jalur (overlay: `onClose`/`onSubmit` callback; native: emit + tutup window) + fallback web `leaveHashRoute()`; (4) mode web tanpa backend: `client.ts` pesan jelas + `helloWorld/appInfo/sourcesList` lokal, `sourceStore.load` tak reset pilihan saat daftar kosong; (5) shell responsif: sidebar drawer + backdrop ≤900px, drawer auto-tutup saat overlay buka, sheet filter lokal MainPage dihapus (pindah ke OverlayHost). Verifikasi: `pnpm check` 0/0, `pnpm test` 15 pass, `pnpm build:frontend` OK, modul dev-mode di Vite 200 tanpa error. |
| 2026-09-20 | Muse | Config-driven (bundle = nhentai) + beranda tak nyangkut mode search | Done | **(1) Koreksi dugaanku**: 3 config bundel memang **sengaja** dihapus user — config-driven, sumber dari repo `kuron-extensions` lewat Ekstensi. Dihapus ulang (`git rm`) dan tes yang bergantung bundle dipindah ke fixture temp-dir: `config.rs::write_fixture` + `bundled_nhentai_only_and_overlay_wins` (bundle=1; overlay menimpa + menambah id baru), `search_form_params_keep_config_order` (uji mekanisme `preserve_order`), `remote.rs::MANGADEX_FIXTURE` (potongan asli endpoint+queryRules → assert URL eksak tetap jalan). `docs/task-parity-search-filter-options.md` dikoreksi (cek lama **4 file** itu basi → 1 file; jangan `git checkout HEAD --` config itu, jangan kembalikan tes ke `bundled_dir()`); baseline 47 pass/9 ignored. **(2)** Keluhan halaman utama menampilkan UI search → **BUKAN bug**: user konfirmasi itu perilaku yang diinginkan. Auto-apply dipertahankan & diperluas: efek MainPage membuka mode pencarian tersimpan **juga saat app baru dijalankan** (dulu mount pertama selalu beranda); tombol Bersihkan kini `clearSavedQuery` (buang query, `values` form tetap) + `contentStore.clearSearch()` supaya pencarian tak balik sendiri saat app dibuka ulang. Verifikasi: `cargo test --lib` 47 pass/9 ignored, `pnpm check` 0/0, `pnpm test` 7 pass. |
| 2026-09-20 | Muse | Akar "data beda" mobile vs desktop (query `raw:` multi-value) | Done | Laporan user: query filter yang sama → hasil beda. Akar: `md_endpoint_raw` merge raw dulu `merged.retain(ek != k)` **per nilai** ⇒ kunci multi-value tinggal NILAI TERAKHIR: `excludedTags[]` 1 dari 4, `originalLanguage[]=zh-hk` saja (bukan 5), `availableTranslatedLanguage[]=en` saja (bukan 2). Bukti live API MangaDex: URL lama total **263** (100 item pertama semua `zh-hk`), URL utuh ala mobile total **43.304** (ja/id), **overlap 0/100**. Fix: kumpulkan `raw_keys` + `raw_out`, `retain` sekali lalu `extend` ⇒ nilai raw menumpuk utuh, kunci raw tetap menggusur default template. `rawParam`/`order[x]=y` & `apply_query_rules` tak berubah. Tes `user_filter_query_builds_full_mangadex_url` kini `assert_eq!` URL **EKSAK** (kunci regresi). Frontend sudah benar (`buildParts` kirim 1 pasangan per nilai). SALAH DUGA lalu dikoreksi user: 3 config bundel memang **sengaja** dihapus (config-driven → dari Ekstensi), bukan ulah proses paralel — lihat baris di atasnya. `cargo test --lib` **47 pass/9 ignored**, `pnpm check` 0/0, `pnpm test` 5 pass. Perlu restart `pnpm dev` agar fix Rust ikut terkompilasi. |
| 2026-09-20 | Muse | Auto-apply filter saat ganti source | Done | Akar: effect MainPage selalu `load()`+reset search. Kini: simpanan `{values,query,label}` (`filterPersist.ts` + 5 test node, `pnpm test`); FilterPage autosave tiap perubahan; ganti source auto-search bila ada query (mount pertama tetap home). Mobile juga reset (ValueKey per source)—kita lebih baik. `pnpm` 0/0 + build OK. |
| 2026-09-20 | Muse | Simpanan filter per-sumber + search modal | Done | `kuron.filter.<source>` di localStorage: simpan saat Cari, restore saat buka (survive ganti source + buka-tutup), reset hapus simpanan. Search modal: ikon lup + tombol clear + focus ring di wrapper ala SourceList. `pnpm` 0/0 + build OK. |
| 2026-09-20 | Muse | Rapi modal picker (tanpa NeedMCP) | Done | NeedMCP tak tersedia di sesi (tanpa CLI/key); pakai prinsip tercatat yoru-sumi + token Kuron. Modal: heading grup disembunyikan bila satu-satunya grup "other", hint cari dinamis (gabungan "Cari tag…", tunggal "Cari {label}…"), empty-state "Tidak ditemukan". `pnpm` 0/0 + build OK. |
| 2026-09-20 | Muse | Fix field MD hilang: flex-shrink collapse | Done | Akar: `form.body` flex-column overflow → shrink dibebankan hanya ke `.picker-row`/`.group` (`overflow:hidden` ⇒ `min-height:0`) → tinggi 0 = garis kosong. Fix 1 aturan `.body>*{flex-shrink:0}` di FilterPage. `pnpm` 0/0 + build OK, rule terkonfirmasi di bundel. Verifikasi visual oleh user (screenshot). |
| 2026-09-20 | Muse | Parity search/filter MangaDex | Done | Bundle `mangadex/ehentai/hitomi-config.json` (tag `/manga/tag` + queryRules out-of-the-box); `md_endpoint_raw` ganti-total per kunci (contentRating 1 nilai gusur 4 default), `rawParam` ditempel utuh `order[x]=y`, `apply_query_rules` (hasAvailableChapters + multi); REST `with_source_file`; FilterPage picker fallback Enter + `ui.multi`; `cargo` 45 pass/8 ignored (`repro_md_rating` live hijau, curl erotica 6256), `pnpm` 0/0. |
| 2026-09-20 | Muse | Modal hilang-hilang: HMR churn + boundary | Verified | Browser and native Tauri repro with a quiet tree both mount the tag dialog on tap. Supplied screenshot was from the older pre-picker render; bundled and installed MangaDex configs are byte-identical. Temporary `[DEBUG-filter-picker]` logs removed. `svelte:boundary` remains to expose runtime errors instead of blank UI. |
| 2026-09-20 | Muse | Filter native window -> inline bottom sheet | Done | MainPage now opens FilterPage in the current window as a bottom sheet with backdrop; FilterPage accepts optional `onClose`, preserving native-window fallback for `#filter`. `pnpm check` 0/0. |
| 2026-09-20 | Muse | Platform-specific filter presentation | Done | `isTauriRuntime()` routes Tauri desktop to the native `filter` WebviewWindow and browser mode to the inline bottom sheet. `pnpm check` 0/0; `pnpm build:frontend` green. |
| 2026-09-20 | Muse | ZIP extension source ID validation | Done | Instalasi `kuron-config-providers.zip` gagal pada `config/manga18.club-config.json` karena validasi lama menolak semua titik. `is_safe_component()` kini hanya menolak kosong, `.`/`..`, separator path, dan control character; domain ID seperti `manga18.club` diterima. ZIP tetap staged/all-or-nothing dan kasus traversal tetap ditolak. Regression `install_zip_accepts_domain_style_source_ids` lulus; `cargo test --lib ...::install_zip` 2 pass; `pnpm check` 0/0. |
| 2026-09-20 | Muse | ZIP selective install + icon preview | Done | ZIP tidak lagi langsung memasang semua config. File/URL kini dipreview dulu sebagai checklist sumber; hanya ID terpilih dikirim ke `cmd_extension_install_staged_zip`. `ui.iconPath` relatif seperti `./images/manga18.png` dibaca dari ZIP dan dikonversi ke data URL, lalu ikon ditampilkan di checklist dan disimpan sebagai meta ikon saat instalasi. `pnpm check` 0/0, frontend build OK, regression ZIP backend lulus. |
| 2026-09-20 | Muse | UI hapus sumber terpasang | Done | Halaman Ekstensi kini punya bagian `Terpasang` yang membaca `sourceStore.available`, menampilkan ikon/nama/version, dan tombol `Hapus` per sumber. `nhentai` tetap terkunci sebagai bawaan aplikasi. `pnpm check` 0/0. |
| 2026-09-20 | Muse | Picker tag gabungan ala mobile + urutan config | Done | Kontrak mobile via subagent (DFU dibaca penuh): pairing include/exclude = substring nama/queryParam (tanpa key config khusus), opsi dari sumber included, sheet tanpa tab + siklus none→include→exclude, grup format/genre/theme/content/other, auto-AND mode. Desktop: FilterPage dapat deteksi picker+pairing, baris ringkas + chip inline deletable, modal cari+grup+reset+terapkan(+N −M) utk gabungan & tunggal; `serde_json/preserve_order` + tes urutan (field tak lagi alfabetis); token success/warning/info light. `pnpm check` 0/0, `cargo test --lib` 40/6fail(sandbox)/9ign. Sisa: Filter Lanjutan lipat, restore filter tersimpan, FilterItem tags. |
| 2026-09-20 | Muse | Filter hilang grup tag: T3 parsial + fresh popup | Done | Grup tag (4 field `ui.dataSource`) tak tampil di popup user padahal disk+backend+template benar → kemungkinan window basi (openPopup dulu fokus-saja). Fix: `cmd_search_form` sertakan `error` per-dataSource (tak lagi telan); FilterPage tampilkan error + tombol Muat ulang + baris status `N field · ds:count`; `openFilterWindow` selalu buat-ulang window. Restore `hitomi-config.json` (dihapus proses paralel). `pnpm check` 0/0, `cargo test --lib` 39/6fail(sandbox)/9ign, live mangaTags 77 ok. |
| 2026-09-20 | Muse | Cek mangaTags MangaDex (belum fix) | Open | API+config+backend mapping BENAR: curl `/manga/tag` 77 item; tes live baru `live_mangadex_mangatags_resolves_77` hijau (77 opsi value/label/group); installed config identik bundel. Dugaan utama: app user basi (restart `pnpm dev`) atau fetch gagal ditelan jadi `[]` (butuh T3). Anomali: fetch reqwest 8.27 dtk vs curl instan. |
| 2026-09-20 | Muse | Fix filter NHentai 0 hasil | Done | Filter kirim `raw:query=..&sort=..`, tapi `NhentaiApiAdapter::search` tak urai prefix `raw:` (scraper/REST bisa) → kueri harfiah `raw:query=..` → API balas `total:0` (curl bukti). Kini `search_url()` murni: urai raw (query+sort, `tag_id`→endpoint `tagSearch`), kosong→allGalleries. Tes regresi + live API hijau. Catatan: 6 tes gagal sandbox (`TcpListener` bind ditolak), 1 tes bundle gagal karena sesi paralel tambah 3 config bundel (WIP, bukan dari fix ini). |
| 2026-09-20 | OpenCode | Port paket generik mobile | Done | Dari `kuron_generic`: rantai image-fallback (data-src/data-lazy-src/pagespeed + placeholder `*_result`/pagespeed_static) + regex dotAll group1-else-0; `fill_url` (encode query, drop param kosong ala UrlBuilder — cegah 400 sekelas nhentai); rate-resolve minDelay→rps→rpm ala factory. Factory dedicated→generik + pipeline/referer dicatat untuk Fase 5/6. `cargo test` 36 pass/6 ignored, live NHentai+MangaDex hijau. |
| 2026-09-20 | OpenCode | Zip via file picker | Done | Link saja kurang → `tauri-plugin-dialog` + `cmd_extension_install_zip_file` (filter .zip, batal = info) + tombol "Pilih file .zip…" (URL tetap ada). Perlu restart `pnpm dev` (plugin + capability). Hijau semua. |
| 2026-09-20 | OpenCode | Hapus panel Backend status | Done | User: panel `Kuron Desktop v0.1.0 · tauri-v2` + form Sapa dihapus dari MainPage (+ CSS + state mati). Versi tetap di footer sidebar + Tentang. Spec app-shell diselaraskan. `pnpm check` 0/0. |
| 2026-09-20 | OpenCode | Pisah search/filter + empty state | Done | Repro: EH `language:english` 25 item (sehat) vs MD 0 (teks=judul). Kebenaran: MD butuh Filter (bahasa/tag), bukan grammar di header. UI: mode text/filter, bar + jumlah hasil, dot hanya filter, banner sembunyi, panel kosong eksplisit. Hijau semua, spec valid. |
| 2026-09-20 | OpenCode | Filter popup NeedMCP | Done | User: panel inline jelek. Saran NeedMCP (yoru-sumi modal: header/body/footer, pill badge, radius 10/12, focus ring) diterjemah ke token Kuron di popup window `#filter` 560×720 (konsisten source-picker/about) + dot filter-aktif + event `filter-submit`. Panel inline dihapus. `pnpm` 0/0, spec valid. Restart `pnpm dev` (capability windows + route baru). |
| 2026-09-20 | OpenCode | Implement 4c search per-sumber | Done | Spec→kode: `raw:` di scraper+REST + test; `cmd_search_form` (+resolve dataSources endpoint→options) + `cmd_search` via `repo_for` (bug mock-only) + `cmd_tag_query` (mapping murni, chip UI defer—entity tanpa tags); UI SearchPanel dinamis + header cari aktif + bar Hasil/Bersihkan/Kueri + loadMore cabang search. Live: EH `raw:f_search` + MD `raw:title` hijau. `cargo` 41 pass/0 warn, `pnpm` 0/0, openspec valid. Restart `pnpm dev` (command+capability? command baru tanpa capability—restart karena handler). |
| 2026-09-20 | OpenCode | Spec search per-sumber (4c) | Done | Jawab pertanyaan user: tiap sumber punya `searchForm` unik (text/tag/select/page + dataSources) → query `raw:k=v` → adapter `_searchRaw`; indikator aktif = header "Hasil Pencarikan"+Bersihkan+bar `Kueri:`. Spec ditulis (4 requirement), tasks 4c TODO (4 item). Temuan: `cmd_search` masih mock-only + engine belum dukung `raw:`. Valid. |
| 2026-09-20 | OpenCode | Cursor token E-Hentai | Done | User: load more tetap mati. Akar via curl: `?page=2` DIABAIKAN situs (isi identik hal 1); mobile next-page via token `#unext→?next=`. Port `PaginationCursors` + `extract_next_url` + `fetch_list_with_next`; live hal1→hal2 item beda hijau. `cargo` 39 pass. Restart `pnpm dev` (Rust). |
| 2026-09-20 | OpenCode | Parity MangaDex+E-Hentai (audit user) | Done | User benar: port menyimpang. (1) `repo_for` hardcode menang atas JSON installed → dibalik. (2) MangaDex: limit 20→100, rating 2→4, home→allGalleries+order, cover `.512.jpg`, parse lengkap, chapters+at-home real (ganti error "nyusul"). (3) EH: home hal>1 ulang URL→isi `{page}` (load more hidup), selector 1:1, enrichment style-cover/`.gt`, detail `#gn/#gd1/#gdd`, Part chapters (reader lazy Fase 5). Live chain MD hijau, `cargo` 38 pass. Sisa jujur: tags/artists tak muat entity (butuh perluasan Content), EH pages lazy Fase 5. Restart `pnpm dev` (Rust). |
| 2026-09-20 | OpenCode | Openspec susulan hardening | Done | Bagus ditanya user: fix batch tak tercatat. Tambah grup 4b (10 item DONE) + spec: app-shell (Ekstensi), splash (navigasi dijamin + boundary + dedupe), content-browse (pagination/home/badge), extensions (zip file + non-blocking + meta-filter). Valid, 31/47. |
| 2026-09-20 | OpenCode | Badge config-driven + dialog fix | Done | Benar: badge hardcode. Kini `pageCount`+`language` dibaca dari fields config (ala mapper mobile; fallback defaultLanguage). Dialog zip: `blocking_pick_file` macetkan runtime → loading abadi; ganti callback + oneshot. Test fixture hijau. `cargo` 37 pass. Restart `pnpm dev` (command berubah). |
| 2026-09-20 | OpenCode | Fix meta kebaca sumber | Done | `load_dir` telan semua JSON → `ehentai-meta`/`mangadex-meta` muncul sebagai sumber. Kini hanya `*-config.json`; test regresi hijau. `cargo` 37 pass. Catatan: ikon HI/EH/MD lama belum ada (install sebelum fitur ikon) → install ulang sekali. Restart `pnpm dev` (Rust). |
| 2026-09-20 | OpenCode | Badge bahasa ganti badge sumber | Done | `Content.language` (ISO): NHentai dari tag_ids/detail-tags (12227→en, 29963→zh, default ja — `languageTagMap` mobile), situs JSON dari `defaultLanguage` (`normalize_lang`). Kartu: badge sumber → chip 🇬🇧 English dkk (`utils/lang`); featured meta ikut. `cargo` 36 pass, `pnpm` 0/0. |
| 2026-09-20 | OpenCode | Pagination + banner + kartu mobile | Done | Pagination ujung-ke-ujung: `home_feed(page)` di trait→mock→impl (mock hal>1 kosong), `home_page_path` pola `homePage`, `cmd_home_feed(source?, page?)`, store `load/loadMore` + dedupe, tombol "Muat lebih banyak". Featured → banner putar 5 terbaru (backdrop blur, dots, auto 6 dtk). Kartu ala mobile: cover full-bleed + scrim + badge halaman (`page_count` NHentai) + badge sumber. Blacklist belum (butuh settings Fase 7). `cargo` 36 pass, `pnpm` 0/0. |
| 2026-09-20 | OpenCode | Fix each_key_duplicate | Done | Feed areakomik isi slug sama 2× (seksi homepage beda) → `each (id)` crash → splash beku 100% (render main gagal). Dedupe by-id di `contentStore.load`; `svelte:boundary` di App dipertahankan + pesan ramah. Hijau 0/0, HMR cukup. |
| 2026-09-20 | OpenCode | Fix stuck splash 100% | Done | `await setMainSize()` di timer akhir: reject/hang IPC = `go("main")` ke-skip. Kini resize fire-and-forget + navigasi sinkron dijamin; `await current()` masuk try. Hijau 0/0, HMR cukup (frontend saja). |
| 2026-09-20 | OpenCode | Fix home feed scraper (areakomik) | Done | Akar: home_feed pakai pola search + query kosong → halaman "Hasil Pencarian" nol kartu (curl bukti). Kini `home_path` dari pola `home` mobile (`home_url()`, fallback list). Areakomik install→feed LIVE hijau. Restart `pnpm dev`. |
| 2026-09-20 | OpenCode | Cover + ikon tampil | Done | Ternyata kartu tak pernah render `<img>` (placeholder huruf pra-Fase 2). Kini: cover live + fallback huruf (`onerror`, lazy, `no-referrer` anti hotlink-guard); tile sumber pakai ikon manifest (`icon_url` disimpan saat install + `InstalledSource`, fallback inisial); teks "mock Fase 0" basi dihapus. Hijau 0/0. Catatan: ekstensi yang sudah terpasang sebelum ini belum punya ikon → install ulang sekali. |
| 2026-09-20 | OpenCode | Engine JSON generik (fix areakomik) | Done | `repo_for` kini baca pola `scraper` JSON ter-install (`source_config_from_json`: container/fields/regex/slug/inherits/detail/chapters/reader) — bukan cuma hardcode 4 situs. Engine dirombak ke `FieldMap` + `extract/extract_self` + `detail_title/cover`. Fixture areakomik (list/detail/chapters/pages) hijau; live areakomik tak terjangkau dari network ini (koneksi 000) — sumber CF-blocked kini warn-log, bukan silent. `cargo test` 33 pass/6 ignored. Restart `pnpm dev`. |
| 2026-09-20 | OpenCode | Fix feed nhentai 400 | Done | API tolak `query=` kosong → `search("")` pakai endpoint `allGalleries` (`/api/v2/galleries?page=`) dari config. Regresi di live test hijau. `cargo test` 32 pass. Restart `pnpm dev` bila sedang jalan (kode Rust). |
| 2026-09-20 | OpenCode | Apply 4.7 ekstensi UI + dinamis | Done | Popup `#extensions` (manifest URL + install/uninstall + zip) + item sidebar; store id-based + migrasi; 5 command baru; `cmd_home_feed(source?)` → `repo_for` (nhentai/mangadex/hitomi/eh via installed, "semua" mock); live manifest install hijau. `cargo test` 32 pass/5 ignored, `pnpm` 0/0 + build OK. Perlu restart `pnpm dev` (capability + command baru). tasks 21/37. |
| 2026-09-20 | OpenCode | Bundel nhentai-only + zip | Done | Klarifikasi: bundel HANYA nhentai (1:1 mobile), sisanya link/zip. `load_overlay` (installed menang), `install_zip_url/bytes` two-pass anti zip-slip. `cargo test` 30 pass/4 ignored, 0 warning, `pnpm` 0/0 + build OK. |
| 2026-09-20 | OpenCode | Pivot 4.6 ekstensi installable | Done | Tanpa bundel di app. `ExtensionManager`: fetch manifest resmi, install JSON + sha256 + tolak traversal, uninstall, `load_installed`; test hijau. tasks 20/37. |
| 2026-09-20 | OpenCode | Apply 4.6 config-driven | Superseded | Sempat bundel 45 JSON + parser + NHentai LIVE hijau, lalu dibatalkan (pivot installable). Engine dipertahankan. |
| 2026-09-20 | OpenCode | Apply grup 4 data real | Done | 4.1 reqwest rustls+jar+DoH CF (UA/cookie test hijau, DoH ignored); 4.2 rusqlite CRUD+migrasi hijau (plugin-sql ditolak: by-pass Application); 4.3 KV JSON + keyring real (test ignored); 4.4 FileCache cap+eviksi + ImageCache layout hijau; 4.5 engine scraper/REST + 3 site config + ContentRepositoryImpl + 4 usecase + 4 command; MangaDex LIVE hijau, NHentai HTML 403 CF-edge pending. `cargo test` 21 pass/3 ignored. tasks 19/35. |
| 2026-09-20 | OpenCode | Tentang resize paksa | Done | Fokus popup lama cuma `setFocus` (ukuran lama kekunci) → tambah `setSize` di `openPopup` + branch menu Rust. Hijau semua. |
| 2026-09-20 | OpenCode | Tentang width 560 | Done | Popup about 480×800 → 560×800 (`lib.rs` + `openAboutWindow`). Perlu restart `pnpm dev`. cargo check bersih. |
| 2026-09-20 | OpenCode | Hapus tab sumber konten | Done | Tab Semua/NHentai/Hitomi/E-Hentai + CSS `.tabs` dihapus dari MainPage; sumber cukup dari sidebar (popup). `pnpm check` 0/0, build OK. Spec `app-shell` diselaraskan. |
| 2026-09-20 | OpenCode | Apply grup 2+3 roadmap | Done | 2.1 tracing subscriber + test; 2.2 AppState `Arc<dyn ContentRepository>` + `cmd_home_feed(State)` + blanket Arc; 2.3 `.github/workflows/ci.yml` matrix 3 OS (YAML valid, hijau runner nunggu push); 3.1 roundtrip serde 4 test; 3.2 ts-rs 12 derive 15 tipe + `export_types_ts` tulis `types.ts` (`type`, bukan `interface`); 3.3 async-trait + command async. `cargo test` 11 pass, `cargo check` bersih, `pnpm check` 0/0, build OK. AGENTS domain rule → serde+ts-rs. tasks 14/35. |
| 2026-09-20 | OpenCode | Openspec payung roadmap | Done | Change `kuron-desktop-roadmap`: proposal + 13 spec delta + design + tasks. `validate` valid, 4/4 artefak. Planning only, tanpa ubah kode. |
| 2026-09-19 | OpenCode | Sidebar mini logo | Done | Collapsed 64px tambah logo maskot `logo_app.webp` 40px rounded+hairline di atas (`mini-logo` button, hover ring coral); klik → expand via `onToggle` baru (MainPage). HMR cukup. Hijau 0/0. |
| 2026-09-19 | OpenCode | Sidebar Tentang → popup | Done | Item `Tentang` (MORE) tadinya dead-end → buka popup `about` 480×800 via `openAboutWindow()` baru; `openPopup()` helper generik dipakai source-picker + about; `activeNav` tidak berubah, error tampil di konten. HMR cukup. Hijau 0/0. |
| 2026-09-19 | OpenCode | Tentang custom + menu macOS | Done | About panel native cuma nama+versi → window popup custom 460×640 (`AboutPage`, hash `#about`): logo, versi live `cmd_app_info`, deskripsi, stack chips, daftar sumber+versi, tombol repo (plugin-opener), footer. Menu `Tentang Kuron` custom via `WebviewWindowBuilder` (fokus bila ada). Cargo description/authors diperbaiki. Capability windows +`about`. cargo + svelte + build hijau. |
| 2026-09-19 | OpenCode | About port mobile 1:1 | Done | `AboutPage` ditulis ulang ikut `about_screen.dart`: hero `logo_app.webp` pulse + pill `v{live}`, PEMBARUAN (cek `releases/latest` repo desktop beneran, status + buka halaman rilis), KOMUNITAS & INFO 6 row + akordeon (lisensi = daftar dep nyata, S&K/Privasi/FAQ = md `assets/legal/id/` render `marked`, donasi = QRIS 151KB + GitHub Sponsors), DIBANGUN DENGAN (Tauri v2/Rust/Svelte 5/Clean Arch), footer Shirokun20 verbatim. Legal diadaptasi desktop (sumber NHentai/Hitomi/E-Hentai/MangaDex, path Pengaturan mobile dihapus, kontak repo desktop). Window 480×800. Hijau semua. |
| 2026-09-19 | OpenCode | MEMORY format mirror kuron-mobile | Done | Struktur disamakan: Identity + Related Repos + Key Features, Architecture Overview (2 tree) + Layer Rules, Progress Dashboard (openspec kosong → debt di Open Issues), Recent Sessions. RTK section mobile tidak dibawa (tooling khusus mobile). |
| 2026-09-19 | OpenCode | Tema Linen Meridian 2-mode, hapus amoled + gradient | Done | NeedMCP `linen-meridian` token light+dark (Zen Season token kosong). Surfaces/text/border diadopsi, primary tetap coral Kuron. Amoled dihapus (store/CSS/token). Semua gradient → flat solid + hairline (no AI slop). Tracking heading -0.02em. |
| 2026-09-19 | OpenCode | NeedMCP wireframe app-shell (Yoru Sumi) | Done | Style `yoru-sumi` locked (guest, per-call slug). Wireframe `gaming-store-catalog` JSON → `Sidebar` 256 + header 64 + tabs 44 + konten 1180 di `MainPage`. Styling 100% token Kuron (directive: struktur saja). `pnpm check` 0/0, build OK. |
| 2026-09-19 | OpenCode | Liquid Glass coba + revert ke solid | Done | Transparent window + blur/saturate panel ala glass di semua kartu; user: jelek → revert total ke solid Kuron. `.pen` tak terbaca (Pencil MCP transport fail); NeedMCP dikonfirmasi component library (butuh API key), bukan .pen reader. Tema sementara: Solid Kuron. |
| 2026-09-19 | OpenCode | Icon Kuron + window resize + logo fit | Done | `frame.webp` 1536 → `app-icon.png` 1024 → `pnpm tauri icon` regen `src-tauri/icons/` (icns/ico/png). Hapus `tauri.svg` sisa template + favicon. Icon dock macOS: binary embed saat compile → `touch build.rs` + recompile. Splash logo gepeng (900×600 dipaksa kotak) → `height:auto`; ring lingkaran → rounded-rect → dihapus; splash kecil (logo 120) grow 1.4× + window 480×700 → 1200×800 saat ke main (`lib/api/window.ts`, capability window). |
| 2026-09-19 | OpenCode | Assets + clean arch + splash→main + AGENTS/MEMORY | Done | Copy `logo_app.webp`, `frame.webp`, 4 fonts manga dari `learn_flutter/nhasixapp`. Rust: core/domain/application/data/commands/network/cache + `cmd_home_feed` mock. Frontend: route store, `SplashScreen` (logo glow, steps, progress, dots), `MainPage` (featured + grid + backend status), `TagChip`, font-face. `AGENTS.md` + `MEMORY.md` awal. `cargo test` 3 pass. |
| 2026-09-19 | OpenCode | Svelte scaffold + tema Kuron exact | Done | Vite 8 + Svelte 5.57 + TS 5.9 (downgrade dari 7, svelte-check ≤6). `tokens.ts` port `colors_const`/`design_tokens`/`tag_color_palette` 1:1; `app.css` var `--kuron-*` + alias shadcn; switcher dark/light/amoled. `spec KURON_TAURI_V2_DESKTOP.md` → `docs/architecture.md`. |
| 2026-09-19 | OpenCode | Hello world clean-arch init | Done | `core/error` + `domain/entities/hello` + `usecase say_hello` + `cmd_hello_world`/`cmd_app_info`; frontend vanilla `client.js`. Fix `generate_handler!` tolak path → `use` import. `cargo test` 2 pass. |
| 2026-09-21 | Muse Code | Route khusus detail/reader + MD tags/stats/related + rail bab/sheet + seni library | Done | Route `detail`/`reader` fullscreen tanpa sidebar (`routeStore.openDetail/openReader`, App boundary per route, MainPage murni). `Content` += `available_languages/description/rating/favorites`, `Tag.id` String (MD UUID). MD: tags grup + availLangs + sinopsis di detail, statistics fail-soft (follows→favorites, rating), related batch 2-fase (`/recommendation` buang self+dedup → `/manga?ids[]`, urut skor), chapters param bahasa (chip dari detail ala `loadChapterLane`, scraper = filter lokal). Detail: hero + sinopsis + tag grup + rail bab kanan sticky (20 + sheet penuh + chip bahasa + Esc) + terkait + komentar. Reader: tile gagal + tombol reload per-halaman & massal, scroller mandiri. Library: header halftone Bangers + badge HAL. `cargo` 68/0/0, `pnpm` 0/0 + 21 pass, openspec 16/16, E2E 19/19. Rust berubah → restart `pnpm dev` sekali. |
