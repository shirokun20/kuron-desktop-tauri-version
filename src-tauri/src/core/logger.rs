//! Logger. Fase 0: no-op — `tracing` subscriber nyusul Fase 0 lengkap.
//! Aturan privasi (spec §17): jangan log prompt/image, glossary cuma on/off.

pub fn init() {
    // TODO(Fase 0): tracing_subscriber::fmt().init()
}
